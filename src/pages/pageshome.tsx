import PropTypes from 'prop-types'
import React, { Component } from 'react'
import Navbarr from '../sharing/navbar'

export class pageshome extends Component {
  static propTypes = {}

  render() {
      return (
      <div>pageshome</div>
    )
  }
}

export default pageshome